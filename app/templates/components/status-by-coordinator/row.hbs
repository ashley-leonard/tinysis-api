<td>{{full-name student}}</td>
<td>{{student.attributes.status}}</td>
{{#each months as |month|}}
  {{status-by-coordinator/column
    student=student
    month=month
    term=term
    studentHash=studentHash
  }}
{{/each}}