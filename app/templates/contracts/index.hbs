{{#check-empty
  count=model.meta.count
  noResultsMessage="No contracts were found with these settings"
}}
  {{count-paragraph
    result=model
    name="contract"
  }}
  {{contracts-list
    contracts=model.data
  }}
{{/check-empty}}