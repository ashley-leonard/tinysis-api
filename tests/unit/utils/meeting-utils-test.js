import meetingUtils from 'tinysis-ui/utils/meeting-utils';
import { module, skip } from 'qunit';

module('Unit | Utility | meeting-utils', () => {
  skip('it works', (assert) => {
    const result = meetingUtils();
    assert.ok(result);
  });
});
