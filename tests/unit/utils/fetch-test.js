import fetch from 'tinysis-ui/utils/fetch';
import { module, test } from 'qunit';

module('Unit | Utility | fetch', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = fetch();
    assert.ok(result);
  });
});
