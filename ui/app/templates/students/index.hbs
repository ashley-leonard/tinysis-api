{{#check-empty
  count=model.meta.count
  noResultsMessage="No students were found with these filters"
}}
  {{count-paragraph
    result=model
    name="student"
  }}
  {{students-list
    students=students
  }}
{{/check-empty}}
